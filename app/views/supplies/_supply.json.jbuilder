json.extract! supply, :id, :grower_id, :consignment_number, :date_received, :ref_no, :value, :paid, :due, :status, :image, :employee_id, :created_at, :updated_at
json.url supply_url(supply, format: :json)
